var GENERATOR_PARAM_TYPES = {
  TIME: 'time',
  HEX: 'hex',
  STRING: 'string',
  ARRAY: 'array',
  BUFFER: 'buffer',
  EMPTY: 'empty'
};

var HEX_CONVERT_STRING_TYPES = {
  ENCODE: 'encode',
  DECODE: 'decode'
};

var constants = {
  GENERATOR_PARAM_TYPES: GENERATOR_PARAM_TYPES,
  HEX_CONVERT_STRING_TYPES: HEX_CONVERT_STRING_TYPES
};

module.exports = constants;
